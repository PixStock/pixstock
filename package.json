{
  "name": "pixstock",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "tsc -b packages/shared packages/agqp packages/vault-crypto packages/pyth-verify packages/tx-policy",
    "build:apps": "npm run build -w @pixstock/web -w @pixstock/vault -w @pixstock/relayer",
    "dev:packages": "tsc -b --watch packages/shared packages/agqp packages/vault-crypto packages/pyth-verify packages/tx-policy",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo apps/web/.next apps/vault/dist apps/relayer/dist"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  },
  "engines": {
    "node": ">=20"
  }
}
